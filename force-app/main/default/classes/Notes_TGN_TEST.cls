/* Generated by TestGen on Mon May 2 2022 at 6:04 PM (GMT) */
@isTest
private class Notes_TGN_TEST{
    @isTest(SeeAllData=true)
    private static void fetchNotes_TGN_TEST(){
        Database.SaveResult dsr;
        Notes__c drNotes_c;
        Test.startTest();
        Notes.fetchNotes();
        Test.stopTest();
    }
    
    @isTest
    private static void completeTask_TGN_TEST(){
        Database.SaveResult dsr;
        Notes__c drNotes_c;
        drNotes_c=new Notes__c();
        dsr=Database.insert(drNotes_c,false);
        Test.startTest();
        Notes.completeTask(new List<String>{drNotes_c.Id});
        //Notes.completeTask(null);
        Test.stopTest();
    }
    @isTest
    private static void createNoteRecord_TGN_TEST(){
        Test.startTest();
        Notes.createNoteRecord('a');
        Notes.createNoteRecord('');
        Test.stopTest();
    }
    @isTest
    private static void Notes_TGN_TEST(){
        Notes obj = new Notes();
    }
}